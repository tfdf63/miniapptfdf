# miniapptfdf

Mini App для tfdf.ru (Vite + React).

## Деплой

Секрет **BEGET_SSH_KEY**: полное содержимое приватного ключа (как из `cat ~/.ssh/id_ed25519`) — от `-----BEGIN` до `-----END` включительно. Используется webfactory/ssh-agent, ключ в файл не пишется.
